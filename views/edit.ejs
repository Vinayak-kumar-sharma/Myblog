<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Post</title>
  <link rel="stylesheet" href="/newstyle.css">
</head>
<body>
  <h1>Edit Post</h1>
  <form action="/edit/<%= post.slug %>?_method=PUT" method="POST">
    <div class="form-group">
      <label for="title">Title:</label>
      <input type="text" id="title" name="title" value="<%= post.title %>" required>
    </div>
    <div class="form-group">
      <label for="description">Content:</label>
      <textarea id="content" name="description" rows="4" required><%= post.description %></textarea>
      <textarea id="content" name="markdown" rows="10" required><%= post.markdown %></textarea>

    </div>
    <button type="submit" class="btn">Save Changes</button>
  </form>
  <a href="/<%= post.slug %>" class="btn">Cancel</a>
</body>
</html>
