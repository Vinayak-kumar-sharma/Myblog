<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= post.title %></title>
  <link rel="stylesheet" href="/poststyle.css">
</head>
<body>
  <header class="header">
    <h1><%= post.title %></h1>
    <p class="post-meta">Published on: <span class="post-date"><%= post.createdAt.toDateString() %></span></p>
  </header>
  <main class="content">
    <article class="c">
      <p class="post-content"><%= post.description %></p>
    </article>
  </main>
  <footer class="footer">
    <a href="/" class="btn back-btn">Back to Home</a>
    <form action="/post/<%= post._id %>?_method=DELETE" method="POST" style="display:inline;">
      <button type="submit" class="btn btn-danger">Delete</button>
    </form>
    <a href="/post/<%= post._id %>/edit" class="btn btn-primary">Edit Post</a>

  </footer>
</body>
</html>
